mutation CreatePerson($name: String!, $dateOfBirth: String!, $bio: String) {
    createPerson(name: $name, dateOfBirth: $dateOfBirth, bio: $bio) {
        id
        name
        dateOfBirth
        bio
    }
}